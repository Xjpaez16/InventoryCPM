package com.example.inventorycpm.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta principal — Azul corporativo oscuro
val PrimaryBlue = Color(0xFF1A56DB)
val PrimaryBlueLight = Color(0xFF3D74F4)
val PrimaryBlueDark = Color(0xFF0F3DA8)

// Acentos
val AccentTeal = Color(0xFF0EA5A0)
val AccentAmber = Color(0xFFF59E0B)

// Fondos
val BackgroundDark = Color(0xFF0F172A)
val SurfaceDark = Color(0xFF1E293B)
val SurfaceVariantDark = Color(0xFF334155)

// Texto
val OnPrimary = Color(0xFFFFFFFF)
val OnBackground = Color(0xFFF1F5F9)
val OnSurface = Color(0xFFE2E8F0)
val OnSurfaceVariant = Color(0xFF94A3B8)

// Semánticos
val SuccessGreen = Color(0xFF22C55E)
val ErrorRed = Color(0xFFEF4444)
val WarningOrange = Color(0xFFF97316)
val NeutralGray = Color(0xFF64748B)

// Estados de factura
val PendienteColor = Color(0xFFF59E0B)
val ConfirmadaColor = Color(0xFF22C55E)

// Fondos claros (para tarjetas)
val CardBackground = Color(0xFF1E293B)
val CardBorder = Color(0xFF334155)
